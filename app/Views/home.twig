<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title|default('MVC Base') }}</title>
    <!-- Bootstrap 5 CSS via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .hero-section {
            background: linear-gradient(135deg, #0d6efd 0%, #084298 100%);
            color: white;
            padding: 80px 0 100px;
            border-radius: 0 0 3rem 3rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            position: relative;
            overflow: hidden;
        }
        .feature-card {
            background: white;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05), 0 2px 4px -1px rgba(0,0,0,0.03);
            transition: all 0.3s ease;
            height: 100%;
            border: 1px solid rgba(0,0,0,0.05);
        }
        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
            border-color: rgba(13, 110, 253, 0.2);
        }
        .icon-wrapper {
            width: 64px;
            height: 64px;
            background: rgba(13, 110, 253, 0.1);
            border-radius: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.75rem;
            color: #0d6efd;
            margin-bottom: 1.5rem;
            transition: all 0.3s ease;
        }
        .feature-card:hover .icon-wrapper {
            background: #0d6efd;
            color: white;
        }
        .code-container {
            background: #212529;
            border-radius: 1rem;
            overflow: hidden;
            box-shadow: 0 20px 25px -5px rgba(0,0,0,0.1);
        }
        .code-header {
            background: #343a40;
            padding: 0.75rem 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }
        .mac-dots {
            display: flex;
            gap: 6px;
        }
        .mac-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
        }
        .dot-red { background: #ff5f56; }
        .dot-yellow { background: #ffbd2e; }
        .dot-green { background: #27c93f; }
        
        pre {
            margin: 0;
            padding: 1.5rem;
            color: #f8f9fa;
        }
        .keyword { color: #ff7b72; }
        .variable { color: #79c0ff; }
        .string { color: #a5d6ff; }
        .function { color: #d2a8ff; }
    </style>
</head>
<body>

    <!-- Hero Section -->
    <header class="hero-section text-center mb-5">
        <div class="container">
            <i class="bi bi-box-seam display-1 mb-3 d-block"></i>
            <h1 class="display-3 fw-bold mb-3">{{ title|default('MVC Base') }}</h1>
            <p class="lead mb-4 fw-normal" style="color: rgba(255,255,255,0.85); max-width: 600px; margin: 0 auto;">
                Um esqueleto PHP <span class="fw-bold">ultrarrápido</span>, projetado para desenvolvedores que amam código limpo, rotas elegantes e validação moderna.
            </p>
            <div class="d-flex justify-content-center gap-3">
                <a href="https://github.com/FelipeOropeza/mvc-estrutura" target="_blank" class="btn btn-light btn-lg px-4 rounded-pill fw-semibold shadow-sm">
                    <i class="bi bi-github me-2"></i>Meu GitHub
                </a>
                <a href="#code-sample" class="btn btn-outline-light btn-lg px-4 rounded-pill fw-semibold border-2">
                    Ver Exemplo
                </a>
            </div>
        </div>
    </header>

    <main class="container mb-5" style="margin-top: -60px; position: relative; z-index: 10;">
        
        <!-- Cards de Benefícios -->
        <div class="row g-4 mb-5">
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="icon-wrapper">
                        <i class="bi bi-rocket-takeoff"></i>
                    </div>
                    <h3 class="h5 fw-bold mb-3">Sustentação Limpa</h3>
                    <p class="text-muted mb-0">Baseado no núcleo do PHP puro sem milhares de dependências travando o Composer. Carregamento em milissegundos.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="icon-wrapper">
                        <i class="bi bi-shield-check"></i>
                    </div>
                    <h3 class="h5 fw-bold mb-3">Attributes Validation</h3>
                    <p class="text-muted mb-0">Esqueça IFs gigantescos. Valide Form Requests nativamente utilizando os novos Atributos do PHP 8.0+ em seus Objetos (DTOs).</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="icon-wrapper">
                        <i class="bi bi-terminal-plus"></i>
                    </div>
                    <h3 class="h5 fw-bold mb-3">Ferramenta CLI (Forge)</h3>
                    <p class="text-muted mb-0">Crie Controllers, Models e Views instantaneamente pelo seu terminal de comando, igual aos frameworks de grande porte.</p>
                </div>
            </div>
        </div>

        <!-- Bloco de Código de Exemplo -->
        <div id="code-sample" class="row justify-content-center">
            <div class="col-lg-10">
                <div class="text-center mb-4 mt-3">
                    <h2 class="fw-bold fs-3">Comece em segundos</h2>
                    <p class="text-muted">A magia do Controller e das Views ligadas de fábrica.</p>
                </div>
                
                <div class="code-container shadow-lg">
                    <div class="code-header font-monospace text-muted small">
                        <div class="mac-dots me-3">
                            <div class="mac-dot dot-red"></div>
                            <div class="mac-dot dot-yellow"></div>
                            <div class="mac-dot dot-green"></div>
                        </div>
                        <i class="bi bi-filetype-php text-primary"></i> <span class="opacity-75">app/Controllers/HomeController.php</span>
                    </div>
                    <pre><code class="font-monospace fs-6">
<span class="keyword">namespace</span> App\Controllers;

<span class="keyword">use</span> Core\Http\Controller;

<span class="keyword">class</span> HomeController <span class="keyword">extends</span> Controller
{
    <span class="keyword">public function</span> <span class="function">index</span>()
    {
        <span class="variable">$data</span> = [
            <span class="string">'name'</span>  => <span class="string">'{{ name|default('Visitante') }}'</span>,
            <span class="string">'title'</span> => <span class="string">'{{ title|default('MVC Base') }}'</span>
        ];

        <span class="keyword">return</span> <span class="function">view</span>(<span class="string">'home'</span>, <span class="variable">$data</span>);
    }
}</code></pre>
                    <div class="bg-white p-4 text-center border-top">
                        <span class="badge bg-success bg-opacity-10 text-success rounded-pill px-3 py-2 fs-6 fw-normal">
                            <i class="bi bi-check-circle-fill me-2"></i>
                            Olá, <strong>{{ name|default('Visitante') }}</strong>! Esta é a saída em tempo real gerada por sua View.
                        </span>
                    </div>
                </div>
            </div>
        </div>

    </main>

    <footer class="mt-auto py-4 bg-white border-top text-center text-muted">
        <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center">
            <small class="mb-2 mb-md-0">Desenvolvido com MVC limpo e PHP moderno.</small>
            <div class="d-flex gap-3">
                <a href="#" class="text-muted text-decoration-none"><i class="bi bi-book"></i> Documentação</a>
                <a href="#" class="text-muted text-decoration-none"><i class="bi bi-github"></i> Código Fonte</a>
            </div>
        </div>
    </footer>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
